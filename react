import React, { useState } from 'react';
import { ChevronRight, GitHub, Linkedin, Mail, Phone } from 'lucide-react';

const ProjectCard = ({ title, description }) => (
  <div className="bg-white shadow-md rounded-lg p-6 mb-4">
    <h3 className="text-xl font-semibold mb-2">{title}</h3>
    <p className="text-gray-600">{description}</p>
  </div>
);

const SkillTag = ({ skill }) => (
  <span className="bg-blue-100 text-blue-800 text-sm font-medium mr-2 px-2.5 py-0.5 rounded">{skill}</span>
);

const Portfolio = () => {
  const [activeTab, setActiveTab] = useState('about');

  const projects = [
    {
      title: "Voice Assistant with OpenAI",
      description: "Implemented a virtual voice assistant using OpenAI's API, capable of understanding natural language and providing coherent responses."
    },
    {
      title: "Stock Price Prediction",
      description: "Developed an LSTM-based model for stock price prediction, demonstrating proficiency in time-series forecasting with deep learning and financial data."
    },
    {
      title: "Image Captioning using Deep Learning",
      description: "Developed an image captioning model using pre-trained CNNs, transfer learning, and optimization techniques to achieve accurate caption generation."
    }
  ];

  const skills = [
    "Machine Learning", "Deep Learning", "NLP", "Data Visualization", "Statistical Analysis",
    "Python", "R", "SQL", "TensorFlow", "PyTorch", "Keras", "Pandas", "NumPy", "Matplotlib", "Seaborn"
  ];

  return (
    <div className="bg-gray-100 min-h-screen">
      <header className="bg-blue-600 text-white py-8">
        <div className="container mx-auto px-4">
          <h1 className="text-4xl font-bold">Abhay Sharma</h1>
          <p className="text-xl mt-2">Data Scientist & AI Specialist</p>
        </div>
      </header>

      <nav className="bg-white shadow-md">
        <div className="container mx-auto px-4">
          <ul className="flex space-x-4 py-4">
            {['about', 'projects', 'skills', 'contact'].map((tab) => (
              <li key={tab}>
                <button
                  onClick={() => setActiveTab(tab)}
                  className={`px-4 py-2 rounded-md ${activeTab === tab ? 'bg-blue-600 text-white' : 'text-gray-600 hover:bg-gray-100'}`}
                >
                  {tab.charAt(0).toUpperCase() + tab.slice(1)}
                </button>
              </li>
            ))}
          </ul>
        </div>
      </nav>

      <main className="container mx-auto px-4 py-8">
        {activeTab === 'about' && (
          <section>
            <h2 className="text-2xl font-semibold mb-4">About Me</h2>
            <p className="text-gray-700 mb-4">
              Detail-oriented Data Science professional with a strong foundation in Python, R, descriptive analysis, data visualization, and machine learning methodologies. Always searching for deeper meanings and ways to unlock the full potential of data to deliver balanced, insight-driven analyses.
            </p>
            <div className="bg-white shadow-md rounded-lg p-6">
              <h3 className="text-xl font-semibold mb-2">Education</h3>
              <ul className="list-disc list-inside text-gray-700">
                <li>MS Data Science & AI - Himachal Pradesh University (2022-2024)</li>
                <li>BS Chemistry - MCM DAV College (2019-2022)</li>
              </ul>
            </div>
          </section>
        )}

        {activeTab === 'projects' && (
          <section>
            <h2 className="text-2xl font-semibold mb-4">Projects</h2>
            {projects.map((project, index) => (
              <ProjectCard key={index} title={project.title} description={project.description} />
            ))}
          </section>
        )}

        {activeTab === 'skills' && (
          <section>
            <h2 className="text-2xl font-semibold mb-4">Skills</h2>
            <div className="bg-white shadow-md rounded-lg p-6">
              <div className="flex flex-wrap gap-2">
                {skills.map((skill, index) => (
                  <SkillTag key={index} skill={skill} />
                ))}
              </div>
            </div>
          </section>
        )}

        {activeTab === 'contact' && (
          <section>
            <h2 className="text-2xl font-semibold mb-4">Contact</h2>
            <div className="bg-white shadow-md rounded-lg p-6">
              <ul className="space-y-2">
                <li className="flex items-center">
                  <Phone className="mr-2" size={20} />
                  <span>+91 7018941218</span>
                </li>
                <li className="flex items-center">
                  <Mail className="mr-2" size={20} />
                  <a href="mailto:datadynamos3@gmail.com" className="text-blue-600 hover:underline">datadynamos3@gmail.com</a>
                </li>
                <li className="flex items-center">
                  <GitHub className="mr-2" size={20} />
                  <a href="https://github.com/mystdream" target="_blank" rel="noopener noreferrer" className="text-blue-600 hover:underline">github.com/mystdream</a>
                </li>
                <li className="flex items-center">
                  <Linkedin className="mr-2" size={20} />
                  <a href="https://linkedin.com/in/abhaysharma38" target="_blank" rel="noopener noreferrer" className="text-blue-600 hover:underline">linkedin.com/in/abhaysharma38</a>
                </li>
              </ul>
            </div>
          </section>
        )}
      </main>

      <footer className="bg-gray-800 text-white py-4">
        <div className="container mx-auto px-4 text-center">
          <p>&copy; 2024 Abhay Sharma. All rights reserved.</p>
        </div>
      </footer>
    </div>
  );
};

export default Portfolio;